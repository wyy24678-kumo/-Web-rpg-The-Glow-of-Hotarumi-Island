# 生成《萤见之岛的微光》轻量化 Web 项目结构计划

基于您提供的参考图，我将为您的游戏项目搭建基础的文件目录结构。这仅涉及目录和空文件的创建，不会包含具体的业务逻辑编程。

## 1. 目录结构创建
我将在项目根目录下创建以下文件夹：
- `assets/images/`：用于存放像素美术资源。
- `assets/audio/`：用于存放背景音乐及音效。
- `data/`：用于存放游戏配置及剧情文本。
- `scripts/`：用于存放核心逻辑、存档及 UI 交互脚本。
- `styles/`：用于存放 CSS 样式文件。

## 2. 基础文件初始化
我将生成以下基础文件（仅包含最基本的框架，不涉及功能实现）：
- `index.html`：项目的主入口文件，采用 HTML5 标准。
- `data/story.json`：初始化为空的剧情配置文件。
- `scripts/main.js`：核心逻辑入口脚本（空）。
- `scripts/save.js`：存档管理脚本（空）。
- `scripts/ui.js`：界面交互脚本（空）。
- `styles/main.css`：核心样式表（空）。

## 3. 执行原则
- **不进行编程**：所有 `.js` 和 `.css` 文件将保持为空或仅包含必要的注释。
- **结构对齐**：完全遵循您提供的参考图结构。

请确认该计划，我将立即为您搭建项目骨架。